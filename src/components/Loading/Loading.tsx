const Loading = () => {
    return (
        <div className="z-0  overflow-hidden min-h-full min-w-full items-center justify-center absolute flex">
            <h1 className="text-5xl">Loading...</h1>
        </div>
    );
};

export default Loading;